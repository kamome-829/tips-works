import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      light: "#333333",
      main: "#000000",
      dark: "#000000",
    },
    secondary: {
      light: "#ffa733",
      main: "#ff9100",
      dark: "#b26500",
    },
  },
});

export default theme;
